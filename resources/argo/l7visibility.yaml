apiVersion: "cilium.io/v2"
kind: CiliumNetworkPolicy
metadata:
  name: "l7-visibility"
spec:
  endpointSelector:
    matchLabels:
      namespace: argocd
  ingress:
  - toPorts:
    - ports:
      - port: "8080"
        protocol: TCP